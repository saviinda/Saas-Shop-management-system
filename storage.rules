rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Bank slips, shop logos, receipts & avatar storage
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null || request.resource.size < 10 * 1024 * 1024;
    }
  }
}
